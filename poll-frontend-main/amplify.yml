version: 1
applications:
  - appRoot: poll-frontend-main
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build -- --configuration production
      artifacts:
        baseDirectory: dist/poll-frontend/browser
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*